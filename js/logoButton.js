// Add event listener to SVG to reset color scheme when clicked
document.getElementById('logo-reset-button').addEventListener('click', () => {
    setColourScheme();  // Resets the colors when SVG is clicked
});